import math
if __name__ == '__main__':
    t = 10
    a=[]
    while t >0 :
        b = [int(x)%42 for x in input().split()]
        for x in b :
            a.append(x)
        t -= len(b)
    a=set(a)
    print(len(a))
